# 补货收货、仓间调拨和库存盘点

入口：**库存管理 → 补货 / 调拨 / 盘点**。

本模块管理实物数量，不代替采购付款、批次成本核算或外部 WMS 库存接口。
仅对已经纳入仓库 SKU 映射/台账的商品登记数量；不把人工合作仓的其他商品变成无限库存或强制数量管理。
新 SKU 请先维护仓库产品映射。入库不会新增站点映射、改变路由或开启 Woo 库存自动同步。

## 操作

### 补货收货

1. 选择本地数量仓、SKU、计划数量，填写唯一的补货凭证和说明，保存补货单（不增库存）。
2. 每批实物到货后，从补货单详情点击“登记本批实收”。填写唯一到货批次、完好数量、破损数量。
3. 尚未到货但后续会到的商品，不填“确认少收”。只有确定不会再收到的部分，才填确认少收并说明原因。
4. 另一位有该仓审核权限的人员核对实物/凭证并审核。只有完好数量增加 `inv_stock.on_hand`，已有预留不变。
5. 每批审核后累计完好、破损、少收、待收数量；全部核清后完成，有破损/少收则“差异结案”。

### 仓间调拨

- 本地仓 → 本地仓：调拨提交不扣库存；审核调出后扣源仓可用库存并进入在途，不能占用订单预留。目标仓分批提交实收、审核后入库。
- 外部 WMS / 不管本地数量的人工合作仓 → 本地仓：调出审核只确认对方已调出的凭证，写单据审计，不修改对方库存/本地镜像，也不发送 WMS 指令。对方需自行确认其系统的调出数量。目标仓照实审核入库。现网新波兰仓（API）属于这种凭证调出模式；它未维护本地 SKU 数量，明细按目标中转仓的受管 SKU 填写。
- 调拨在途的数量不属于目标仓可售库存。未到数量不能直接删除；最终少收/破损通过收货差异明确结案。
- 调入外部 WMS 或不管数量的人工合作仓不在此模块执行，避免本地台账和外部真源冲突。

### 库存盘点

- 页面只显示所选仓受管 SKU；可搜索、勾选当前筛选结果、批量填写**实盘总数**。
- 实盘按同一截止时间核对仓内实物和已打包未出库商品。预留仍是现存的一部分，不能拿“可用”代替账面现存。
- 页面加载账面现存、预留、最近流水快照；提交和审核均校验快照。期间有出入库/预留变化，须驳回旧单、重新核对实物再提交，不能覆盖新库存。
- 审核自动计算 `实盘 - 原现存` 并记盘盈/盘亏流水。零差异也保留盘点审核记录，不生成虚假库存增减。
- 实盘低于订单预留时阻止入账，先核查受影响缺货订单。系统不会擅自取消订单、释放预留或改仓。
- 审核后查看库存台账和补货建议；已有缺货履约单仍按现有“重新分仓”流程处理，不自动重发历史订单。

## 权限

新增 `inv_operation_permissions`，按用户 × 仓库分开授权：补货/收货提交、调拨提交、盘点提交、审核入账。

- 内置 `admin` 才能通过本页“按仓授权”授予/撤销这些权限；授权变化写审计。
- 现有 `can_manage_inventory` 库存负责人继续拥有全仓能力；在按仓面板取消勾选不会撤销原全仓权限。
- 所有审核禁止自审，包括内置管理员。至少需要另一位库存负责人审核。
- 迁移只为**已有该仓发货授权且仓库库存真源为 local** 的人员新增收货/盘点提交权；不授予审核、调拨或全仓库存权限。
- 按仓操作授权和现有仓库查看授权都能打开库存入口，修复独立授权用户无法进入库存台账的问题。
- 旧的直接调整和采购单一键收货接口在迁移后返回 409，避免绕过审核；历史采购单/流水保留。

## 状态和安全约束

| 单据 | 合法过程 |
|---|---|
| 补货单 | open → partial → completed / completed_with_difference；无有效收货时可 cancelled |
| 调拨单 | submitted → in_transit → partial → completed / completed_with_difference；审核前可 rejected / cancelled |
| 收货单、盘点单 | submitted → approved / rejected / cancelled |

允许一次收全跳过 partial。已入账/调出不做物理删除、覆盖或自动冲销；纠错新建关联说明的盘点/反向调拨单。

- 防重复：请求 key + 内容指纹，活动凭证唯一索引，同一上游同时只允许一张待审核收货单。
- PostgreSQL：请求 advisory lock、父单/单据行锁、库存行锁；SQLite：BEGIN IMMEDIATE。审核和库存流水在同一事务中提交，多行中任何错误都回滚。
- 重复相同审核只读返回原结果；不重复增减库存。接口结果不明时保留原请求重试，先检查原单据，不要重建同一实际到货。
- 普通发货和批准入账在同一库存行锁上串行化；预留/出库在锁内重新验证数量及防重标记。
- 单据、逐 SKU 明细、提交人、审核人、意见、时间、状态变更和库存前后值都可追溯。无 WMS 下单、客户邮件或订单状态写入。

## 迁移、验收与回滚

在代码所在目录运行 `python inv_workflows_schema.py`；生产 PostgreSQL 必须使用数据库维护连接，不能给应用账号放开 public 建表权限。
此脚本兼容 SQLite / PostgreSQL，只新增四张表及索引，补充按仓提交权限；不修改现存、预留、订单、批次成本或同步开关。维护账号建表后，仅向应用账号授予以下权限（本环境应用角色为 `woo_analysis_app`）：

```sql
GRANT SELECT, INSERT, UPDATE ON inv_documents, inv_operation_permissions TO woo_analysis_app;
GRANT SELECT, INSERT ON inv_document_lines, inv_document_events TO woo_analysis_app;
GRANT USAGE, SELECT ON SEQUENCE inv_documents_id_seq, inv_document_lines_id_seq,
    inv_document_events_id_seq TO woo_analysis_app;
```

应用账号不能 UPDATE/DELETE 审计记录或明细，不能删除单据，也不获得 CREATE SCHEMA / CREATE TABLE 权限。
不要用旧 SQLite 文件对生产 PostgreSQL 执行迁移。

发布前保存目标代码文件、工作区差异和 PostgreSQL 备份。迁移后验证：原库存/订单未被迁移改变；新单据为空；发货员只有原本地仓的提交权限；无全仓写权限新增；服务重载后验证页面和 API。

回滚优先关闭新入口/停止新单据操作并修复，保留所有单据和审计。不要删表、不要恢复旧全库备份覆盖发布后订单。
如果仍无任何新单据，可回退本次代码并保留新增表；新增表存在时需保留旧直接入库/调整接口的审核拦截，不能恢复无审核写库存路径。
若已经有入账，不能仅通过代码回滚撤销库存；应先对账，用反向单据修正。

测试：`python -m pytest tests/test_inventory_workflows.py -q`。
隔离 PostgreSQL 测试必须同时设置 `WOO_DB_NAME_OVERRIDE` 与 `INVENTORY_WORKFLOW_TEST_PG_DATABASE`，名称一致且以 `woo_inventory_test_` 开头。每项使用独立 schema；绝不能对生产库跑此测试。
浏览器验收可设置 `INVENTORY_WORKFLOW_BROWSER` 为测试浏览器可执行路径；独立本地测试库、测试会话，不使用操作者的真实登录会话。
# 临时库存修改与市场缺货核对（2026-09-09）

- 系统设置 → 临时库存修改授权（用户管理中也有入口）：仅内置管理员可选人员、本地数量仓、到期时间和原因，可随时撤销；不预置任何授权。
- 获授权人员：库存管理 → 补货 / 调拨 / 盘点 → 临时修改库存（自动盘点入账）。填写实盘总数、盘点编号、差异原因并确认。不要把“可用库存”当作实盘总数。
- 系统在同一事务内生成盘点单、写入库存流水、标记“系统自动审批”。提交人是真实操作人，审批依据是授权编号和授权人，不冒充管理员人工审批。
- 到期、撤销、跨仓、无授权均不可自动入账。盘点期间发生出入库/预留变化，或实盘低于预留时阻止提交；保留原库存，要求重新核对。重复请求不重复入账。
- 普通盘点/收货/调拨仍走原审批规则；临时授权不包含审核其他人单据、全仓管理、站点同步设置或发货权限。
- 管理员在盘点单列表查看全部库存调整，详情有修改前后数量、原因、操作人、自动审批和授权依据；临时授权页保留授权/撤销记录。
- 发货员在既有市场/站点可见范围内看到缺货商品，包括整单尚未分配的缺货订单。已分配商品仍受仓库范围限制；同一订单行的已分配及缺货数量合并显示，不增加虚构商品行。单独排除站点继续生效。
- 这里只调整展示和库存授权，不替客户发货、改仓、补发通知，也不自动启用库存同步。库存入账后按原流程核对并重新分仓。

数据库升级：先显式运行 `inv_temporary_access_schema.py`；只新增临时授权表/索引。生产通过 PostgreSQL 维护账号执行，再只给应用账号该表 SELECT/INSERT/UPDATE 和序列 USAGE/SELECT；不授予 CREATE、DELETE 或审计表 UPDATE 权限。

回滚：恢复本次变更的代码，临时授权表与已产生盘点/库存流水保留。已入账库存不能用旧数据库备份覆盖，应以新的盘点更正单修正。
