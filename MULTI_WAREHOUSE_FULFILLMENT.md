# 多仓履约实施说明

## 业务规则

- 自 2026-07-29 起，匈牙利 WMS 开发、路由和自动提交暂停；历史适配器
  保留用于审计和将来恢复，但不能给新波兰仓复用。
- 新波兰仓是独立的外部 WMS 仓，只履约明确映射的 4 款商品。专属 SKU
  不允许缺货时静默回落到金毅/金谷或匈牙利仓。
- 其余商品统一由波兰人工合作仓（金毅/金谷）履约，适用于波兰、捷克和
  匈牙利市场。
- 波兰仓是“人工合作仓”：金毅/金谷安排实际发货，库存数量和缺货不在
  OMS 维护；波兰/捷克/匈牙利未映射商品会自动建立仅供人工履约的内部
  标识并默认归属该仓。该标识不含 WMS 条码，不能提交任何外部 WMS。
- 捷克和匈牙利市场可由金毅/金谷使用 Packeta 发货；捷克市场由钱品宏
  在现有发货管理中录入 Packeta 运单。物流查询模板为
  `https://tracking.packeta.com/en/{tracking}`。发货动作写入 AST 并由
  WooCommerce 通知客户，不要求波兰合作方维护 SKU 或库存。
- 同一订单可拆为多个 `Fulfillment`，每个仓库可有独立 `Shipment` 和运单号。
- 跨仓 COD 订单中，波兰仓包裹收取波兰商品金额和订单全部客户运费；
  匈牙利 WMS 包裹只收取分配到匈牙利仓的商品金额，不重复收取运费。
- 两仓 COD 按 WooCommerce 商品行优惠后含税金额分配，波兰仓承担订单
  级运费、费用和舍入差额；两仓 COD 合计必须严格等于订单总额。
- 客户运费逐订单读取 WooCommerce `shipping_total` 和 `currency`，
  不写死匈牙利站的 3490 HUF，也不在两个仓库之间平均。
- 匈牙利仓储费和运输费不进入客户 COD，记录为
  `monthly_statement`，按供应商月底账单单独核对。
- 只有匈牙利仓履约的 COD 订单仍由匈牙利仓正常代收。
- 缺货不自动换货或取消；订单进入红色缺货 + 人工处理状态。
- 匈牙利仓映射外部 `HU01`，渠道代码为完整字符串 `欧洲直发-25`。
- 单箱 10kg 仅为运营建议，不在系统中硬拦截。
- 新波兰仓 API 使用华磊/SZ56T 协议，订单服务地址为
  `http://175.178.192.240:8082`，标签打印服务地址为
  `http://175.178.192.240:8089`。两者目前均为明文 HTTP；生产默认拒绝
  使用，只有业务负责人明确接受风险并设置
  `SZ56T_ALLOW_INSECURE_HTTP=1` 后才允许请求。
- 新波兰仓 COD 代收、客户运费归属和供应商结算方式尚未确认；在确认前
  新仓真实发单安全门保持关闭。

## 聚合关系

`Order -> OrderItem -> Fulfillment -> FulfillmentItem -> Shipment -> ShipmentItem -> TrackingEvent`

- `Order` 是 WooCommerce 商业订单。
- `Fulfillment` 是一个仓库对该订单当前 revision 的履约责任。
- `Shipment` 是实际包裹；每个包裹独立同步 AST/WooCommerce 并通知客户。
- 所有非取消包裹都 `delivered` 后，订单聚合才进入 `delivered`，随后幂等同步 WooCommerce `completed`。

## 上线开关

### 金毅金谷临时中转仓（2026-08-19起）

- 仓库编码 `PL-JYJG-TRANSIT`，库存权威为本地有限库存，不是
  原波兰合作仓的 `manual_partner` 无限可分配模式。
- 迁移 017 按调拨表建立 59 个 SKU，每个 10 支，合计 590 支，
  期初数量全部写入 `inv_movements` 审计流水。
- PL/CZ/HU 的四个 Fumot 系列只在口味与调拨 SKU 唯一匹配时
  进入该仓；未调拨口味继续显示缺货，不回退到无限库存。
- 分仓时增加预留数，取消/重分仓时释放，金毅、金谷或钱品宏
  录入 Packeta 运单号时扣减现存和预留；重复运单不重复扣减。
- 路由开关为 `oms_jyjg_transit_routing_enabled`。9 月底新线路
  上线时先关闭此开关，再开启新 WMS；开关只影响后续新规划，
  已生成的临时仓履约单保持原仓，避免重复发货。


迁移 006 与 010 默认写入：

- `oms_fulfillment_enabled=0`
- `oms_auto_plan_enabled=0`
- 匈牙利集成 `auto_submit=0`
- `oms_hungary_wms_development_paused=1`
- `oms_hungary_wms_routing_enabled=0`
- `oms_new_pl_wms_routing_enabled=0`
- 新波兰仓集成 `is_enabled=0`、`auto_submit=0`

因此迁移和服务部署本身不会自动分配历史订单，也不会生成真实 WMS 出库单。应在“多仓履约 → 配置”按以下顺序开启：

1. 波兰/捷克/匈牙利普通商品使用金毅/金谷现有发货管理，不依赖库存主档。
2. 确认 4 款专属商品在每个站点的商品 ID、变体 ID、SKU 和条码。
3. 配置 4 款商品到 `inv_skus`、站点映射和新波兰仓独占归属。
4. 配置新波兰仓生产账号、运输方式 `product_id` 和取消接口独立 `auth`；
   适配器已覆盖认证、创建、结果核验、运单、标签、轨迹、发货标记和取消。
   对方文档没有库存查询和状态回调接口，因此库存按合作方人工反馈维护，
   运单与轨迹由任务轮询。
5. 给多仓履约员工配置行级仓库权限；现有捷克人工发货继续使用站点/
   国家权限，避免看到波兰站订单。
6. 开启后台履约任务，手工规划少量测试订单。
7. 开启新同步订单自动分仓。
8. 确认新波兰仓受控测试和审计日志后，再单独开启新仓路由和自动提交。

系统分别显示人工合作仓、匈牙利 WMS 和新波兰仓的就绪状态。人工合作仓
可在没有数量库存时规划；匈牙利 WMS 的自动提交仍要求 HU01 库存满足。
新波兰仓没有库存 API，4 款专属商品使用 `partner_reported` 库存权威，
但仍强制要求 SKU 主档、站点映射、独占仓库归属、中英文品名和条码完整，
不会把自动建立的人工合作仓商品误下发外部 WMS。

## 外部系统与数据方向

- WooCommerce → 本系统：订单、商品、地址、付款/COD、站点来源。
- 波兰合作方人工确认 → 分仓器：普通商品默认分配金毅/金谷。
- 合作方人工库存反馈 → 分仓器：仅对 4 款明确映射商品生效，缺货不跨仓降级。
- 本系统 → 新波兰仓 WMS：认证、创建订单、发货标记和取消；新波兰仓 WMS
  → 本系统：订单号、面单 URL、运单和官方轨迹。对方未提供库存接口和状态
  回调，本系统使用轮询补齐延迟生成的运单与轨迹。
- 官方 WMS + InPost/Track718 → `TrackingEvent`：事件去重并防止乱序回退。
- 本系统 → AST/WooCommerce：每个包裹单独同步；部分发货不完成整单，最终妥投后才同步 `completed`。

## 失败恢复

- 所有外部操作通过 `oms_integration_jobs` 执行，包含唯一幂等键、租约、指数退避、最大重试和死信状态。
- WMS 创建请求超时视为“结果未知”：匈牙利仓按确定性 `invoiceCode`
  查询；新波兰仓按确定性 `order_customerinvoicecode` 查询。核验前不盲目
  重复创建，无法确认时进入人工恢复。
- 新波兰仓取消使用独立 Open API `order.cancel`。取消超时或连接中断属于
  结果未知，不自动重复取消，转人工核验，避免重复/乱序操作。
- 外部请求写入脱敏的 `oms_external_api_calls`，状态变化写入不可变 `oms_domain_events`。
- COD 代收责任和月结方式写入 `oms_fulfillment_financials`，匈牙利
  仓储费、运输费及账单月份可在月底对账时回填。
- 商品金额、客户运费、订单级调整和 WooCommerce 原始总额由迁移 008
  增加不可变计划快照；旧规则履约单必须重新分仓后才能提交 WMS。
- 重复物流事件按包裹 + 指纹去重；迟到事件保留审计，但不能使包裹状态回退。
- 日常对账检查 WooCommerce 完成状态与本地聚合状态的差异，写入 `oms_reconciliation_issues`。
- Worker 遇到临时 SQLite 写锁时保留任务并等待下一轮，不退出服务。

## 回滚

1. 如需停止临时中转仓新分配，先将
   `oms_jyjg_transit_routing_enabled=0`；不删除已有履约、库存和流水。
2. 迁移 `down 017` 只停用该仓的新路由，保留所有可对账数据。
3. 先关闭其他上线开关并停止 `woo-fulfillment-worker.service`。
4. 代码回退到部署前分支/提交。
5. 如需撤回新波兰仓隔离路由且尚未产生履约数据，可执行
   `inv_migrations.py down 010`。
6. 如只需撤回人工合作仓库存模式和 Packeta 配置，可执行
   `inv_migrations.py down 009`。
7. 如需继续回退商品/运费拆分快照，可执行 `inv_migrations.py down 008`。
8. 如需继续回退 COD/月结字段且尚未产生财务记录，可执行
   `inv_migrations.py down 007`。
9. 如尚未向 WMS 提交任何真实出库单，可继续执行
   `inv_migrations.py down 006` 删除 V2 表。
10. 如已提交真实出库单，只回退代码并保留审计表；外部 WMS 行为不能
   通过数据库回滚撤销。新波兰仓可尝试调用 `order.cancel`，但必须取得
   独立 `auth`，且可取消状态和时限仍需对方确认；接口拒绝或结果未知时
   仍须在物流群联系其运营人员人工拦截并完成对账。
