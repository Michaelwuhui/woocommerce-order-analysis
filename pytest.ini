[pytest]
testpaths = tests
python_files = test_*.py
addopts = --strict-markers

# The repository root contains historical test_*.py diagnostic scripts which
# execute against configured WooCommerce sites at import time.  They are not
# tests and must never be collected by the production-safe suite.
